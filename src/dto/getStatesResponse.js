// const config = require('../config');

const getStatesResponseDto = res =>
  // Return an array of objects of the form :
  // [{id:<id of the state>,name:<name of the state>}]
  // No transformation is necessary as of now
  res;

module.exports = getStatesResponseDto;
